# Лабораторна робота 6

### Інформація про команду
- Назва команди:

- Учасники:
Кривош Андрій 
Прошек Тимофій

### Обрана предметна область

Предметна область — інтернет-магазин мотоциклів .

## 📋 Опис проєкту

У межах лабораторної роботи було реалізовано інтеграцію frontend-частини вебзастосунку з backend API на Flask. Створено демонстраційну сторінку, яка за допомогою асинхронного JavaScript (`fetch`, `async/await`) отримує та надсилає дані до API, а також обробляє можливі помилки. Дані оновлюються на сторінці миттєво без перезавантаження.

---

## 📁 Структура проєкту

```
project/
├── templates/
│   ├── base.html          # Базовий шаблон
│   ├── api-demo.html      # Демонстраційна сторінка роботи з API
│   ├── feedback.html
│   ├── shop.html
│   ├── cart.html
│   └── admin/
│       └── orders.html
        └──......  
├── routes/
│   ├── api.py             # JSON API (feedback, orders, products)
│   ├── shop.py
│   ├── admin.py
│   └── feedback.py
├── models.py              # Робота з базою даних SQLite
├── app.py                 # Головний файл Flask
├── db.sqlite              # База даних
└── README.md              
```

## 🔌 API Endpoints

У роботі використовується власне API, реалізоване у Flask, яке повертає та приймає дані у форматі JSON.

### GET /api/feedbacks

Отримує список усіх відгуків.

**Відповідь:**

```json
[
  {
    "id": 1,
    "name": "Анонім",
    "message": "Чудовий сервіс",
    "created_at": "2025-05-01 12:30:00"
  }
]
```

---

### POST /api/feedbacks

Створює новий відгук.

**Тіло запиту:**

```json
{
  "name": "Іван",
  "message": "Все сподобалось!"
}
```

**Успішна відповідь:**

```json
{
  "status": "success"
}
```

---

### DELETE /api/feedbacks/{id}

Видаляє відгук за ідентифікатором.

**Відповідь:**

```json
{
  "status": "deleted"
}
```

---

### GET /api/products

Отримує список товарів (використовується для створення замовлень).

**Відповідь:**

```json
[
  {
    "id": 1,
    "title": "Yamaha R1",
    "price": 18999,
    "category": "Sport"
  }
]
```

---

### GET /api/orders

Отримує список замовлень разом з товарами.

**Відповідь:**

```json
[
  {
    "order": {
      "id": 1,
      "customer_name": "Гість",
      "status": "new",
      "created_at": "2025-05-01 13:10:00"
    },
    "items": [
      {
        "title": "Yamaha R1",
        "price": 18999,
        "quantity": 1
      }
    ]
  }
]
```

---

### POST /api/orders

Створює нове замовлення.

**Тіло запиту:**

```json
{
  "customer_name": "Гість",
  "items": [
    {
      "product_id": 1,
      "quantity": 2
    }
  ]
}
```

**Відповідь:**

```json
{
  "status": "success",
  "order_id": 3
}
```

---

### PATCH /api/orders/{id}

Оновлює статус замовлення.

**Тіло запиту:**

```json
{
  "status": "processing"
}
```

**Відповідь:**

```json
{
  "status": "updated"
}
```

## ✅ Висновки

Під час виконання лабораторної роботи я навчився інтегрувати frontend з backend API за допомогою Fetch API та асинхронного JavaScript. Було реалізовано отримання, додавання, редагування та видалення даних без перезавантаження сторінки. Я закріпив знання з роботи з JSON, HTTP-методами та обробкою помилок. Також отримав практичний досвід налаштування CORS та організації клієнт–серверної взаємодії у Flask-застосунку.

---

